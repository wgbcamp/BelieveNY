import React, {useEffect} from 'react'

const donate = () => {

    useEffect(() => {
        window.scrollTo(0,0);
    }, []);

    return(
        <div>

        </div>
    )
}

export default donate;